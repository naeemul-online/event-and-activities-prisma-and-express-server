enum UserRole {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum EventStatus {
    OPEN
    FULL
    CANCELLED
    COMPLETED
}

enum ParticipantStatus {
    JOINED
    PENDING
    CANCELLED
}

enum PaymentStatus {
    PENDING
    UNPAID
    PAID
    FAILED
}
