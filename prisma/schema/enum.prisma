enum UserRole {
    USER
    HOST
    ADMIN
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum EventStatus {
    OPEN
    FULL
    CANCELLED
    COMPLETED
}

enum ParticipantStatus {
    JOINED
    PENDING
    LEFT
}

enum PaymentStatus {
    PENDING
    PAID
    CANCELLED
    REFUNDED
}
