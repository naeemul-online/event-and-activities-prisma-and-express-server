model Profile {
    id       String  @id @default(uuid())
    fullName String
    bio      String?
    image    String?
    location String?
    userId   String  @unique
    user     User    @relation(fields: [userId], references: [id])
}
